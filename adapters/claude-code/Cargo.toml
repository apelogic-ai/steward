[package]
name = "steward-adapter-claude-code"
edition.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true
version.workspace = true

[dependencies]
reqwest.workspace = true # Validates governed endpoints without performing I/O.
serde_json.workspace = true # Renders the isolated MCP configuration without shell interpolation.
steward-ports = { path = "../../crates/steward-ports", version = "=0.0.0" }

[dev-dependencies]
steward-types = { path = "../../crates/steward-types", version = "=0.0.0" }

[lints]
workspace = true
