[package]
name = "steward-adapter-codex"
edition.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true
version.workspace = true

[dependencies]
reqwest.workspace = true # Validates the configured inference endpoint without performing I/O.
serde_json.workspace = true # Safely quotes the governed tool endpoint in generated TOML.
steward-ports = { path = "../../crates/steward-ports", version = "=0.0.0" }

[dev-dependencies]
steward-types = { path = "../../crates/steward-types", version = "=0.0.0" }

[lints]
workspace = true
