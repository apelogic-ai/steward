# Generated by `cargo xtask generate-manifests`; do not edit.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: agentruntimes.agents.apelogic.ai
spec:
  group: agents.apelogic.ai
  names:
    kind: AgentRuntime
    plural: agentruntimes
    shortNames:
    - ar
    singular: agentruntime
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for AgentRuntimeSpec via `CustomResource`
        properties:
          spec:
            properties:
              agentType:
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              bindings:
                items:
                  type: string
                nullable: true
                type: array
              budget:
                properties:
                  currency:
                    type: string
                  monthlyLimit:
                    type: string
                required:
                - currency
                - monthlyLimit
                type: object
              llms:
                items:
                  properties:
                    model:
                      type: string
                    provider:
                      type: string
                  required:
                  - model
                  - provider
                  type: object
                type: array
              owner:
                type: string
              principal:
                properties:
                  actingUser:
                    type: string
                  kind:
                    enum:
                    - user
                    - service
                    type: string
                  name:
                    type: string
                required:
                - kind
                type: object
                x-kubernetes-validations:
                - message: user principals require actingUser; service principals require name
                  rule: >-
                    (self.kind == 'user' && has(self.actingUser) && !has(self.name)) || (self.kind
                    == 'service' && has(self.name) && !has(self.actingUser))
              tools:
                items:
                  properties:
                    action:
                      type: string
                    provider:
                      type: string
                    resource:
                      type: string
                  required:
                  - action
                  - provider
                  - resource
                  type: object
                type: array
              ttl:
                type: string
            required:
            - agentType
            - budget
            - llms
            - owner
            - principal
            - tools
            - ttl
            type: object
          status:
            nullable: true
            properties:
              conditions:
                items:
                  description: >-
                    Condition contains details for one aspect of the current state of this API
                    Resource.
                  properties:
                    lastTransitionTime:
                      description: >-
                        lastTransitionTime is the last time the condition transitioned from one status
                        to another. This should be when the underlying condition changed.  If that is not
                        known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: >-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      type: string
                    observedGeneration:
                      description: >-
                        observedGeneration represents the .metadata.generation that the condition was
                        set based upon. For instance, if .metadata.generation is currently 12, but the
                        .status.conditions[x].observedGeneration is 9, the condition is out of date with
                        respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: >-
                        reason contains a programmatic identifier indicating the reason for the
                        condition's last transition. Producers of specific condition types may define expected
                        values and meanings for this field, and whether the values are considered a
                        guaranteed API. The value should be a CamelCase string. This field may not be empty.
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                format: int64
                type: integer
              phase:
                enum:
                - Pending
                - Admitted
                - Provisioning
                - Running
                - Suspended
                - Terminating
                - Terminated
                - Failed
                type: string
              refs:
                properties:
                  litellmKey:
                    nullable: true
                    type: string
                  sandbox:
                    nullable: true
                    type: string
                  workspace:
                    nullable: true
                    type: string
                type: object
              specDigest:
                type: string
              spend:
                nullable: true
                properties:
                  currency:
                    type: string
                  observedAmount:
                    type: string
                required:
                - currency
                - observedAmount
                type: object
            required:
            - conditions
            - observedGeneration
            - phase
            - refs
            - specDigest
            type: object
        required:
        - spec
        title: AgentRuntime
        type: object
    served: true
    storage: true
    subresources:
      status: {}
